<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>FitHub - Profile Page</title>
</head>
<body>
    <h1>Welcome <%= user.name %>! Let's Get After It</h1>
    <form action="/users/profile/<%=user.id%>?_method=PUT" method="POST">
    New Workout: <select name="workout">
        <% for ( let i = 0; i < workout.length; i++ ) { %>
            <% let selected = ( i == 0 ) ? "selected" : ""; %>
            <option value="<%=workout[i].id%>" <%=selected%>><%=workout[i].name%>
            </option>
       <% } %>
    </select><br><br>
    
        <input type="submit" name="" value="Add This Workout"/>
    </form><br><br>

    <h3>My Recently Added Workouts:</h3>
    <% let count = 0 %>
    <ul>
        <% for (let i=(user.Workouts.length - 1); count < 10; i--){ %>
                <li>
                <a href="/workouts/<%=user.Workouts[i].id%>"><%=user.Workouts[i].name%></a>
                </li>
                <br>
                <% count++ %>
        <% } %>
    </ul>

    <br><br>
<footer>
    <a href="/users/profile/<%=user.id%>/edit">Edit Profile</a><br><br>
    <a href="/workouts">View All Workouts</a><br><br>
    <a href="/users">Log Out</a><br><br>
    <h4>Account Active Since: <%=user.createdAt%></h4>
</footer>
    

</body>
</html>